## Langchain text convertion and JSON format

## install virtual environment
python3 -m venv venv

## activate virtual environment
source venv/bin/activate

## prerequistes command
pip install -r requirements.txt

## add .env with enviorment varibles
ANTHROPIC_API_KEY

## install langchain core
pip install -U langchain

## install langchain anthropic
pip install -U langchain-anthropic

## install all the core dependencies
pip install -U langchain-core langchin-community langchain-anthropic

## run the script
python3 Convert_format.py# Langchain_Lang_Conversion
